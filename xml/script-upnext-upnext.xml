<?xml version="1.0" encoding="UTF-8"?>
<window>
	<defaultcontrol always="true">3012</defaultcontrol>
	<onload>Dialog.Close(fullscreeninfo,true)</onload>
	<onload>Dialog.Close(videoosd,true)</onload>
	<controls>
		<control type="group">
			<include condition="String.IsEqual(Skin.String(nextuplayout),fancy_common)">upnext_fancy_common</include>
			<include condition="String.IsEqual(Skin.String(nextuplayout),fancy_triangle)">upnext_fancy_triangle</include>
			<include condition="String.IsEqual(Skin.String(nextuplayout),fancy_default)">upnext_fancy_default</include>
			<include condition="String.IsEqual(Skin.String(nextuplayout),simple_common)">upnext_simple_common</include>
			<include condition="String.IsEqual(Skin.String(nextuplayout),simple_small) | !Skin.String(nextuplayout)">upnext_simple_small</include>
			<include condition="String.IsEqual(Skin.String(nextuplayout),simple_default)">upnext_simple_default</include>
		</control>
		
		
		<include content="swan_debug_overlay">
			<param name="textleft" value="
    $INFO[Window.Property(fanart)] - tvshow fanart
    $INFO[Window.Property(clearlogo)] - tvshow clearlogo
    $INFO[Window.Property(clearart)] - tvshow clearart
    $INFO[Window.Property(landscape)] - tvshow landscape
    $INFO[Window.Property(poster)] - tvshow poster
    $INFO[Window.Property(thumb)] - thumb
    $INFO[Window.Property(plot)] - episode plot
    $INFO[Window.Property(tvshowtitle)] - episode tvshow title
    $INFO[Window.Property(title)] - episode title
    $INFO[Window.Property(season)] - episode season number
    $INFO[Window.Property(episode)] - episode episode number
    $INFO[Window.Property(year)] - episode preimiered year
    $INFO[Window.Property(rating)] - episode rating
    $INFO[Window.Property(seasonepisode)] - season/episode
    $INFO[Window.Property(playcount)] - playcount
	$INFO[Window.Property(remaining)] - remaining
	$INFO[Window.Property(endtime)] - endtime
	$INFO[Window.Property(runtime)] - runtime
	test $INFO[Window.Property(studio)] - studio
"/>
</include>
	</controls>
</window>