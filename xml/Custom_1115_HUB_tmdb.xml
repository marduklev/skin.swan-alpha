<?xml version="1.0" encoding="UTF-8"?>
<window type="window" id="1115">


	group (header with search)
		image background 
		grouplist horizontal ( a | b | c | ... | search)
	
			
	
		<grouplist vertical="123456">
			group
				image (bg) | filter bar
			 
			
			group template (poster~1)
				grouplist horizontal (panel static items vs button controls)
					container heading label(e.g. popular) | group itemfilter1 (e.g. movies)| group itemfilter2 (e.g. shows)| group itemfilter2 (e.g. in cinema)...
				
				group fixedlist|panel
					itemlayout/focusedlayout 
						poster
						 indicator (exist in library)
						 watched overlay (circle indicator)
						 rating ovelray
						
						
						item tag 3 rows
						spacer h
				
				group scrollbar horizontal	(1)
			
			
			
			group template (2)
				
				group 
					group image (artwork container background)
					group image (artwork overlay container background)
			
				grouplist horizontal  (panel static items vs button controls)
					container heading label(e.g. popular) | itemfilter1 (e.g. movies)| itemfilter2 (e.g. shows)| itemfilter2 (e.g. in cinema)...
				
				group fixedlist|panel
					itemlayout/focusedlayout 
						poster
						 indicator (exist in library)
						 watched overlay (circle indicator)
						 rating ovelray
						
						
						item tag 3 rows
						spacer h
						
				group scrollbar horizontal (2)		
			group template (3)
				
				group 
					
					group image (diffuse/gradient or whatever container background)
			
				grouplist horizontal  (panel static items vs button controls)
					container heading label(e.g. popular) | itemfilter1 (e.g. movies)| itemfilter2 (e.g. shows)| itemfilter2 (e.g. in cinema)...
				
				group fixedlist|panel
					itemlayout/focusedlayout 
						poster
						 indicator (exist in library)
						 watched overlay (circle indicator)
						 rating ovelray
						
						
						item tag 3 rows
						spacer h
						
				group scrollbar horizontal	(3) 
		</grouplist>
		
	scrollbar vertical - for grouplist 123456
	<controls>
	
	
	
	</controls>
</window>